# 🔄 대화 핸드오프

## 1. 프로젝트 / 주제
디지털 집사(Digital Butler) MVP 개발 — Phase 0 공통 UI + Phase 1 랜딩 페이지 완료, Phase 2 준비 단계

## 2. 배경 및 맥락
- **프로젝트**: 50~70세 중장년이 카톡 한 줄로 디지털 문제를 해결받는 AI+인간 하이브리드 도우미 서비스
- **작업자**: 비개발자 외식 프랜차이즈 사업자 (쉬운 설명 선호, 한국어 주석 필수)
- **프로젝트 경로**: `C:\Users\Administrator\OneDrive\바탕 화면\06_개발IT\item001-deploy\`
- **GitHub**: `kkl127kkl-lang/business-opportunity` (기존 창업 아이템 1500선 사이트 위에 디지털 집사 개발 중)
- **기술 스택**: Next.js 14 + React 18 + TypeScript 5 + Tailwind CSS 3.4 + Supabase + Claude API + 카카오톡 채널 API + 토스페이먼츠 + Vercel
- **Git 전략**: main → dev → feature 브랜치. main 직접 push 절대 금지
- **개발 워크플로우**: vibe-coding-plugin 스킬 체인 사용 (planner → git → executor → tester → reviewer → doc-sync → git commit)
- **사용자 요청**: "최대한 대시보드는 띄우지말고 해줘" — 브라우저 프리뷰 최소화, 터미널 내 작업 선호
- **DDD 설계**: 도메인 5개(Chat, Counseling, Family, Payment, Auth), 엔티티 5개(User, ChatSession, ChatMessage, Ticket, Subscription), 유스케이스 3개(send-message, ai-respond, gift-subscription)
- **유니버설 디자인**: 별도 "시니어 모드" 없이 기본 디자인 자체가 접근성 확보 (44px 최소 터치, 16px 기본 폰트)

## 3. 완료된 내용

### 이전 대화에서 완료 (사업계획서 + 기획)
- 사업계획서 v2 (2,017건 데이터 포인트 기반)
- PRD v2 (docs/PRD.md)
- PPT v2 (15슬라이드)
- 프로젝트 초기 구조 생성 (project-init)
- DDD 문서 작성 (도메인 5 + 엔티티 5 + 유스케이스 3)
- 코딩 컨벤션 보완 (함수명 규칙, 주석 규칙)
- 마스터 체크리스트 수립 (Phase 0~8)

### 이번 대화에서 완료

#### Phase 0: 공통 UI
- Button 컴포넌트 (5가지 variant: primary/secondary/kakao/outline/ghost, 3가지 size, fullWidth, forwardRef)
- Card 컴포넌트 (hover 애니메이션, highlighted 링 보더)
- SectionWrapper 컴포넌트 (bg: white/gray/cream, 반응형 패딩)

#### Phase 1: 랜딩 페이지 MVP (9개 섹션)
- Header: 고정 헤더 + 스크롤 감지 (useEffect + cleanup)
- Hero: "키오스크가 어려우세요? 카톡 한 줄이면 됩니다" 메인 카피 + CTA 2개 + 신뢰 배지 3개
- Pain: 고통 공감 카드 4개 (2,017건 데이터 출처)
- Service: 3단계 흐름 + 카톡 대화 목업 UI
- Category: 17개 카테고리 그리드 (3/4/5열 반응형)
- Family: 분할 레이아웃 (폰 목업 + 기능 리스트)
- Pricing: 4개 요금제 (충전제 10,000원 ~ 패밀리 49,900원)
- Testimonial: 고객 후기 3건
- Footer: 3열 푸터

#### 테스트
- 39개 테스트 전체 통과 (Button 11개, Card 6개, 랜딩 22개)
- vitest + @testing-library/react + jsdom 환경
- 단위(33) + 통합(3) + 시나리오(3) 테스트

#### 코드 리뷰
- 5영역 검수 통과 (기능/품질/보안/크로스파일/문서)
- header.tsx 스크롤 리스너 메모리 누수 수정 (useEffect 미사용 → useEffect + cleanup 적용)

#### 문서 동기화 (doc-sync)
- CHANGELOG.md: Phase 0 + Phase 1 완료 기록
- PROGRESS.md: Phase 0 공통UI + Phase 1 전체 항목 체크 완료
- PRD.md: 화면 1 랜딩 페이지 "✅ 구현 완료" 표시
- ARCHITECTURE.md: 실제 생성된 16개 파일 구조 반영

#### Git 작업
- 커밋: `feat: Phase 0 공통 UI + Phase 1 랜딩 페이지 MVP` (40 files, +10,533 lines)
- 머지: `feature/landing-page-mvp` → `dev` (no-ff 머지)
- 브랜치 정리: feature/landing-page-mvp 삭제
- 원격 push: dev 브랜치 → origin 완료

### 해결한 기술 이슈
- npm peer dependency 충돌 → `--legacy-peer-deps` 플래그로 해결
- @vitejs/plugin-react v6 ESM 비호환 → v4로 다운그레이드
- vitest.config.ts ESM 에러 → vitest.config.mts로 확장자 변경
- @testing-library/dom 누락 → 별도 설치
- preview_start가 해려본 대시보드(3000)에 연결 → 3001 포트로 분리

## 4. 미결 항목
- Phase 0 나머지: Supabase 프로젝트 생성 + DB 스키마 설계 (아직 미착수)
- Phase 0 나머지: 환경 변수 설정 (.env.local) (아직 미착수)
- 공통 UI Input, Modal 컴포넌트 (Phase 2 진행 시 필요하면 추가)
- npm install 시 `--legacy-peer-deps` 필요 (peer dependency 충돌 때문)

## 5. 다음 단계
1. **Phase 0 마무리**: Supabase 프로젝트 생성 + DB 스키마 설계 (User, ChatSession, ChatMessage, Ticket, Subscription 엔티티 기반)
2. **Phase 0 마무리**: .env.local 환경 변수 설정 (SUPABASE_URL, SUPABASE_ANON_KEY 등)
3. **Phase 2: 인증 시스템** — Supabase Auth + 카카오 소셜 로그인 + 역할 기반 접근 제어
4. **Phase 3: 카카오톡 AI 챗봇** — Webhook + Claude API + 17개 카테고리 자동 분류
5. **Phase 4: 상담원 대시보드** — 에스컬레이션 큐 + 대화 히스토리 + 통계
6. **Phase 5: 가족 PWA** — 부모님 연결 + 활동 요약 + 구독 선물
7. **Phase 6: 결제** — 토스페이먼츠 PG + 구독/충전제
8. **Phase 7: QA + Phase 8: 최종 보고**

> 모든 Phase는 반드시 vibe-coding-plugin 스킬 체인을 따릅니다:
> planner → git(브랜치) → executor → tester → reviewer → doc-sync → git(커밋/머지)
